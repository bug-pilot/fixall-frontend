# FixAll Home Service — Frontend

This project was bootstrapped with Vite (React + JavaScript).

## Setup

1. Install dependencies

   ```bash
   npm install
   ```

2. Run development server

   ```bash
   npm run dev
   ```

3. Run linter

   ```bash
   npm run lint
   ```

## Notes

- Routing and UI will be added incrementally as part of the guided build.
- ESLint and Prettier are configured with recommended defaults.
